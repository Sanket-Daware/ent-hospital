{
  "name": "hospital-client-workspace",
  "private": true,
  "workspaces": [
    "user"
  ],
  "scripts": {
    "dev": "npm run dev -w user",
    "build": "npm run build -w user",
    "lint": "npm run lint -w user",
    "preview": "npm run preview -w user"
  }
}
